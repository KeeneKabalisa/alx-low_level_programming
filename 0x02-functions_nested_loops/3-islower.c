#include "main.h"
#include <stdio.h>

/**
 * _islower - finds out if char is lowercase
 *
 */

int _islower(int c)
{
	if (islower (c))
	{
		return (1);
	}

	else if (islower (c))
	{
		return (0);
	}
}
